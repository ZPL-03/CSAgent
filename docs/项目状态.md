# CSAgent 项目状态

## 当前事实

- 项目目标为复合材料外压圆柱耐压壳智能设计。
- 主流程为耐压壳多智能体智能设计编排。
- `config/app_config.yaml` 的用户可见产品名为 `CSAgent 多智能体智能设计平台`，`CSDM_cph` 仅作为内部包名、目录名、环境变量前缀和仓库命名空间。
- LLM 配置读取项目根目录 `.env`，主模型使用 `LLM_PRIMARY_URL`、`LLM_PRIMARY_API_KEY`、`LLM_PRIMARY_MODEL_NAME`，回退模型使用 `URL`、`API_KEY`、`MODEL_NAME`。
- LLM 后端按主模型优先、回退模型补充的顺序调用；候选生成和报告解释会记录 `llm_call_trace` 事件，事件只包含后端名称、模型名称、状态、耗时、返回字符数和错误摘要，不包含 URL、密钥或提示词正文。
- GUI 顶部当前模型标识读取 `llm_call_trace` 事件，显示本次调用实际使用的领域主模型、回退模型或失败状态；同一状态会写入工作台运行日志和监控日志。
- 监控页提供运行审计入口；LLM 健康检查由本地审计脚本或人工动作触发，检测结果不展示 URL、密钥或提示词正文；运行审计报告写入 `data/results/run_audit_<RUN_ID>.md`。
- `scripts/release_audit.py` 是本地质量检查入口，覆盖产品命名、本地运行产物隔离、密钥忽略规则、知识库运行时路径、UI 展示资产、案例编号连续性、最新报告标题、GUI 深浅主题渲染可选检查和 LLM 连通性可选检查。
- PyQt6 主界面默认为简体中文，“设置”页支持 English、跟随系统、深色工程和亮色工程；界面偏好写入 `data/runtime/ui_settings.json`，不进入 Git。主程序启动时优先加载 `HarmonyOS Sans SC`、`Noto Sans SC` 和 Windows 中文字体，避免中文控件和可视化图注显示为方块。
- PyQt6 主界面采用 `QMainWindow + QSplitter + QStackedWidget` 工程工作台布局：顶部为系统身份栏、页面导航和当前模型标识；语言和主题选择位于“设置”页；左栏展示六个智能体状态、任务队列和知识连接；中央 Stack 包含工作台、知识库、监控、设置；工作台中部上方为自绘 LangGraph DAG，`ORCHESTRATOR` 节点承载需求解析、任务编排和人工确认，不单独显示第七个“需求解析”智能体；下方为对话优先交互区；右栏展示实时视口、会话指标、运行日志、阶段报告、会话数据导出和报告打开入口。实时视口在本机 OpenGL 可用时使用 PyVista 交互视图，支持旋转、缩放、平移、双击重置、按钮重置视角和适配窗口。设置页提供主/回退 LLM、Abaqus、候选来源比例、人工确认节点、RAG/KG 分块和向量索引等本地 YAML 配置表单；右栏展示主模型、回退模型、Abaqus、RAG/KG、候选来源比例和人工确认节点的配置健康状态；密钥仍由 `.env` 管理，GUI 不显示密钥正文。
- 顶部品牌标识显示为 CSAgent 图形徽标、`CSAgent` 主标题和“多智能体智能设计平台”副标题；界面不包含登录头像。DAG 和左侧智能体卡片支持完成、运行中、失败、等待四类状态灯；DAG 节点、状态灯、知识检索支线和文字块按节点几何中心对齐。工作台对话流按内容自适应气泡宽度，短消息不拉满面板，用户输入显示在右侧并保留头像标识，工具调用卡片和证据 chips 与消息正文保持紧凑间距。
- 右侧实时结果区的“导出数据”入口写入 `data/results/session_export_<RUN_ID>.json` 和 `data/results/session_trace_<RUN_ID>.csv`，用于复核当前会话任务、候选、初筛、FEM、知识回流、报告元数据和 TMP-C-CASE 追踪关系。
- 工作站领域模型接口示例为 `LLM_PRIMARY_URL=https://csllm.ipen03.com/v1` 和 `LLM_PRIMARY_MODEL_NAME=csllm`。
- GUI 主流程只使用自然语言设计需求，需求中必须明确包含候选池总数和初筛保留数。
- 工作台从同一会话状态读取结构化任务、用户已给事实、几何约束、候选生成控制参数、初筛控制参数和事实边界；这些字段只用于展示、候选约束和审计，不由界面改写。
- LLM、案例迁移和 DOE 的初始来源配额按 `2:1:1` 计算，实际来源统计以规则检查后的有效候选为准。
- 任务记录包含 `user_input_facts`，该字段只保存用户自然语言明确给出的事实；明确给出的几何数值默认写入 `fixed_geometry`，作为候选生成和有限元输入必须满足的条件；只有字段附近出现“约、参考、附近、设计中心、左右”等参考语义时，对应字段才写入 `geometry_reference`。候选生成 LLM 读取该字段、合并 RAG/KG 检索片段、LLM 候选数量、输出字段要求和系统候选字段约束。字段约束只限定材料库、参数范围和单位格式，不写成用户事实。
- LLM 候选由自然语言表格解析得到，候选编号不参与材料解析；`geometry_reference` 不覆盖模型候选参数，`fixed_geometry` 覆盖对应候选参数；模型候选行和回答原文保存在候选追踪字段中。
- LLM、案例迁移和 DOE 三路候选按材料、几何变量和铺层表达式去重；结构等价的重复方案不进入候选池，候选池缺口由 DOE 补足。
- 候选生成写入 `generation_audit`，记录三路初始配额、原始输出数、规则有效数、实际进入候选池数量、规则过滤数量、结构去重数量、DOE 补足数量、DOE 采样轮次和过滤原因摘要；该审计随候选池进入 GUI，并通过 `candidate_generation_audit` 事件写入工作流事件库。
- 会话候选使用 `candidate_id=display_name=TMP_N`；进入有限元后正式输入使用 `candidate_id=display_name=C<N>`，并以 `session_candidate_id` 回溯会话候选。案例记录顶层、案例设计和正式有限元结果都保留正式 `candidate_id/display_name`；正式有限元结果文件和案例记录中的 `abaqus_results` 保留会话回溯字段，便于离线重生成报告。
- LLM 候选缺少材料、长度、半径、厚度、alpha、beta 或缺陷比时不进入候选池；案例迁移来源必须提供完整候选几何；DOE 使用参数域生成完整候选并闭合候选池数量。
- 知识库读取本项目 `knowledge/csllm` 内置 RAG/KG、`knowledge/runtime` 用户增量和 `knowledge/chroma_db` 向量索引，支持上传资料、批量解析、token 分块、内容去重、向量索引、实体关系抽取、检索验证、索引重建、知识库快照导出、节点/关系搜索、节点类型过滤、关系类型过滤、拖动、缩放、复位、节点选中和关系审计的知识图谱可视化、证据预览和流水线状态显示。
- `config/app_config.yaml` 中 `project_knowledge` 配置当前指向 `knowledge/csllm` 内置数据、`knowledge/runtime/rag/rag_chunks.jsonl` 用户增量、`knowledge/chroma_db` 的 `csdm_cph_project_knowledge` collection、`knowledge/runtime/kg` 和 `knowledge/runtime/manifest.json`。最终检索入口读取内置数据与用户增量合并后的总 RAG/KG。
- RAG 分块参数为 `chunk_token_size=512`、`chunk_overlap_tokens=64`、`min_chunk_tokens=80`，完全相同 chunk 按 `content_hash` 去重。
- 材料库、参数范围和代理公式已写入 `config/`。
- `workflow/` 已提供 LangGraph 状态图运行时、智能体职责契约、SQLite 事件存储、工具注册和快照恢复接口；现有 PyQt6 主对话流程通过该运行时推进，GUI“监控”页可刷新最近运行记录并恢复运行状态，工作台保留候选池、人工筛选、有限元校核、重置和报告入口。恢复运行状态只读取本地运行库，不重新调用 LLM 或 Abaqus。LangGraph 节点启动、节点完成、工具调用、人工确认和仿真队列事件会实时写入工作台运行日志和监控日志，并同步刷新左侧智能体状态卡片和中央 DAG 状态；对话区只展示任务摘要、确认请求、助手解释和人工输入。
- 有限元校核已接入 `workflow/simulation_queue.py` 作业队列，记录候选入队、运行、成功、失败、正式编号、结果摘要和错误信息。
- 知识回流已作为状态图节点 `persist_knowledge` 独立运行，写入 `knowledge_updates` 快照字段，并通过 `KNOWLEDGE_AGENT` 工具事件审计。
- 工作台读取工作流运行事件、状态快照和有限元队列，展示 LangGraph DAG、候选池、候选来源审计、多智能体对话、当前会话指标、运行日志、阶段报告入口和数据导出入口。监控页展示状态恢复、运行审计、工具调用审计、LLM 调用轨迹、有限元队列和诊断信息，并可导出单次运行审计报告。
- 工作台候选池展示全部会话候选，支持点击候选查看几何预览、来源、代理预测和规则状态；代理初筛后全量候选按评分排序，Top-K 标记为默认送检集合，未入选候选置灰但仍可人工送入有限元校核。
- 工作台候选审计展示候选来源构成、三路初始配额、规则过滤、结构去重和 DOE 补足记录。
- 工作台追踪区展示 TMP 会话候选、正式 C 编号、代理预测、FEM 结果、代理误差、CASE 回流状态和报告纳入状态之间的对应关系。
- 工作台阶段报告区读取当前会话报告或 `data/results/latest_report.md`，展示 Markdown/PDF 路径、LLM 工程解释使用状态和 Markdown 正文预览。
- PyQt6 中央“监控”页读取正式案例库中的 FEM 指标，展示结构面密度与极限压力 Pareto 前沿、最佳极限压力收敛历史、最佳案例指标和正式案例数量；同页保留工作流审计面板和运行日志流。工作流审计面板读取 SQLite 运行库、快照和有限元队列，展示状态图、工具调用审计、LLM 调用轨迹、诊断和导出入口。

## 当前知识库运行时

| 资产 | 路径 |
| --- | --- |
| 内置 RAG 文本块 | `knowledge/csllm/rag/rag_chunks.compact.jsonl.gz` |
| 内置知识图谱实体 | `knowledge/csllm/kg/entities.jsonl` |
| 内置知识图谱关系 | `knowledge/csllm/kg/relations.compact.jsonl.gz` |
| 内置数据清单 | `knowledge/csllm/provenance/manifest.json` |
| 用户上传原始资料 | `knowledge/runtime/uploads/` |
| 用户增量结构化文档 | `knowledge/runtime/structured_text/documents.jsonl` |
| 用户增量结构化文本块 | `knowledge/runtime/structured_text/blocks.jsonl` |
| 用户增量 Markdown 全文 | `knowledge/runtime/structured_text/markdown_documents/` |
| 用户增量 RAG 文本块 | `knowledge/runtime/rag/rag_chunks.jsonl` |
| 向量索引 | `knowledge/chroma_db` / `csdm_cph_project_knowledge` |
| 用户增量知识图谱实体 | `knowledge/runtime/kg/entities.jsonl` |
| 用户增量知识图谱关系 | `knowledge/runtime/kg/relations.jsonl` |
| 用户增量状态清单 | `knowledge/runtime/manifest.json` |

GUI “知识库”页支持上传资料、批量解析、后台入库、索引重建和快照导出。入库流程为 MinerU / Docling 文档解析、语义分块、BGE-M3/Chroma 向量化索引、实体关系抽取和检索验证 / 证据引用；`KnowledgeIngestionService` 在五个阶段发出进度事件，后台 Worker 转发给 GUI，页面以绿、黄、红状态灯实时展示每一步状态和错误信息，并显示内置数据、用户增量、合并 RAG、Vector、合并 KG 数量。Vector 绿色状态只代表向量 collection 实际写入成功，向量后端不可用时保留 JSONL/BM25 和 KG 检索并显示黄色状态。用户增量区为空时仍可读取 `knowledge/csllm` 内置 RAG/KG；增量区单独暴露 0 文档、0 chunk、0 实体关系、分块 token、overlap、去重字段、向量状态字段和五阶段待运行流水线，用于 GUI 状态灯和发布审计。文件选择器与核心解析器共用 `SUPPORTED_QT_FILE_FILTER`，当前覆盖 PDF、DOCX、PPTX、图片、Markdown、文本、JSON/YAML/TOML/INI、CSV、TSV、XLSX/XLSM、Abaqus 输入、子程序、日志、状态、消息和 ODB/SIM/CAE 工程二进制文件。PDF、DOCX、PPTX 和图片优先走 MinerU / Docling；外部解析器不可用时，PDF 使用 PyMuPDF 文本层解析，DOCX/PPTX 尝试文本层解析，图片保留元数据；ODB/SIM/CAE 保留可检索元数据，内部场变量和云图由有限元结果可视化链路读取。新资料入库会与用户增量 `rag_chunks.jsonl`、Chroma collection、`entities.jsonl` 和 `relations.jsonl` 合并；同一资料按文档 SHA 编号替换旧记录，不覆盖其他上传资料或内置数据。检索验证会确认当前资料 chunk 可命中、图谱关系引用有效 chunk，并把证据样本写入 manifest。索引重建复用已解析文档和去重文本块，重建用户增量向量 collection、实体关系、KG 统计和检索验证状态。快照导出写入 `knowledge/runtime/snapshots/knowledge_snapshot_<timestamp>.json`，包含用户增量 manifest、文档、结构化块、RAG 文本块、实体、关系和 KG 统计。GUI 入库链路覆盖上传文件、后台解析、token 分块、overlap、去重、向量索引、KG 抽取、检索验证、状态灯刷新、文档表刷新、可搜索、可按节点类型和关系类型过滤、可拖动/缩放/复位/节点选中的合并知识图谱可视化、图谱审计和证据预览。证据预览融合内置数据、用户增量、向量 collection 和知识图谱关系，用于 LLM 工程上下文和人工核查来源，不参与代理公式计算、排序、有限元校核或案例编号。

## 当前参数域

- `length_mm`: 300-800
- `radius_mm`: 80-180
- `thickness_mm`: 5-20
- `alpha_deg`: 10-80
- `beta_deg`: 10-80
- `imperfection_ratio`: 0.001-0.01

## 当前输出字段

- 候选预测：`asme_linear_buckling_pressure_MPa`、`surrogate_ultimate_pressure_MPa`、`surrogate_PBIPF_MPa`
- 有限元结果：`linear_buckling_pressure_MPa`、`ultimate_pressure_MPa`、`riks_lpf_max`、`visualization_json`、`failure_mode`
- 案例误差：`surrogate_pressure_error_pct`

## 当前案例库

- `data/cases` 包含 `CASE_1` 至 `CASE_15` 共 15 条真实 Abaqus 有限元案例。
- `knowledge/case_library` 包含通过目标校核并可迁移的正式案例 10 条。
- 当前批量建库案例不包含会话追溯 `task_id`；`data/tasks` 仅保存 GUI 或编排器主流程产生的真实任务记录。
- 本轮初始案例使用参数范围内 10 点拉丁超立方采样生成，外压为 30 MPa，极限压力目标为 35 MPa。
- 当前目录中最高正式案例为 `C15 -> CASE_15`，真实 Abaqus 校核状态为 `success`，结论为“通过”；该案例已进入可迁移案例库。
- `data/results` 不保留过期报告；主流程完成报告导出时重新生成 `latest_report.md` 和 `latest_report.pdf`。

## 有限元桥接

- 自动脚本：`abaqus/runtime_build_pressure_hull.py`
- 模板入口：`abaqus/templates/pressure_hull_analysis.py.j2`
- 可选用户子程序参考：本机 `reference/zhangusdfld.for`
- 可选参数化参考：本机 `reference/wangge.py`
- 默认链路：单位外压线性屈曲、一阶模态缺陷云图、Static Riks 后屈曲极限压力
- GUI 可视化：工作台候选池、右侧实时视口和监控页 FEM 指标区读取候选几何、有限元结果和 `visualization_json`；右侧实时视口优先使用 `pyvistaqt` 交互三维几何模型和模态云图，支持旋转、缩放、平移和双击复位；无候选或结果时，真实交互环境显示参考耐压壳模型。OpenGL 交互视图不可用、`QT_QPA_PLATFORM=offscreen`、`CSDM_cph_DISABLE_INTERACTIVE_3D=1` 或离线审计环境中，界面使用与当前主题一致的 Matplotlib 静态工程预览显示参考几何、候选几何或 FEM 模态云图。离线图注跟随界面语言，并展示模态数据点数、面数、标量数量和路径状态。
- 用户子程序：`abaqus.use_user_subroutine=false` 为默认值，显式设为 `true` 后接入 `reference/zhangusdfld.for`

## LLM 调用边界

- `TaskParser` 不调用 LLM，候选池数量、初筛保留数、载荷、几何约束、几何参考和目标压力由规则解析得到。
- `CandidateGenAgent` 调用领域主模型生成 LLM 来源候选；输入为系统整理后的工程任务书，包含用户已给事实、合并 RAG/KG 检索片段、LLM 候选数量、输出字段要求和系统候选字段约束，不把默认压力、默认材料或默认几何范围写成用户事实。
- `ReportGenAgent` 的任务事实、候选编号、压力数值、排序和有限元结论由结构化数据确定性生成；工程解释与制造建议可调用 LLM 做定性解释，输出出现结构化输入之外的新数值、阈值、材料牌号时先做约束清理，清理后仍不合规或调用失败时使用确定性解释。报告模板会清理 LLM 自带的重复章节标题，最终只保留一组“工程解释与制造建议”章节。
- `ReportWidget` 不修改报告内容，不调用 LLM，只预览 `ReportGenAgent` 已生成的 Markdown 内容或最新落盘报告文件。
- `ScreenerAgent`、`FEMAgent`、`KnowledgeAgent`、`ConversationFlowController` 不调用 LLM。

## 工作流运行时

- `DesignWorkflowRuntime.start()` 启动任务并运行到代理初筛人工确认点。
- `DesignWorkflowRuntime.continue_after_confirmation()` 根据当前等待确认项继续推进代理初筛、有限元校核、知识回流或报告导出。
- `DesignWorkflowRuntime.resume()` 通过 `run_id` 读取最近状态快照；`WorkflowEventStore.list_runs()` 为 GUI 提供最近运行摘要。
- `WorkflowEventStore` 写入 `data/runtime/workflow_runtime.sqlite3`，记录工作流运行、节点事件、工具调用事件、人工确认事件和最新状态快照。
- `workflow_runs.status` 由最新快照推导，状态包括 `running`、`waiting`、`paused`、`failed` 和 `completed`；监控页、恢复入口和运行审计使用同一状态字段。
- `ToolRegistry` 将任务解析、候选生成、代理初筛、有限元、知识回流和报告导出注册为可审计工具，工具事件记录输入摘要、输出摘要、耗时和失败原因。
- `SimulationJobQueue` 使用同一 SQLite 运行库记录 `simulation_jobs`，当前为本地串行执行记录，并保留工作站并行作业调度所需的候选编号、正式编号、状态、结果摘要和错误字段。
- `workflow/run_audit.py` 读取工作流快照、事件审计和有限元作业队列，生成 `data/results/run_audit_<RUN_ID>.md`，用于复核候选来源、LLM 调用、FEM 队列、知识回流和阻断诊断。
- 自然语言验收覆盖中文和英文输入：真实任务解析、真实候选生成和真实代理初筛进入运行时人工确认链路；有限元、知识回流和报告阶段在验收环境中通过确定性适配器完成到 `completed`，并检查候选去重、TMP-C 回溯、仿真队列事件、知识回流状态、报告路径和 SQLite 快照。
